import 'package:flutter/material.dart';

class InitialDriverPage extends StatelessWidget {
  const InitialDriverPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("hello"),
    );
  }
}
